server:
  port: 51802
spring:
  profiles:
    active: dev
  application:
    name: medical-device
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
      discovery:
        server-addr: localhost:8848