server:
  port: 51803
spring:
  profiles:
    active: dev
  application:
    name: medical-app
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
      discovery:
        server-addr: localhost:8848